# Backend — MedZip API

Python 3.12+ FastAPI service for DICOM compression/decompression.

## Run locally

```bash
# Install (uv)
uv sync

# Run
uv run uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

See the [project README](../README.md) for full setup and API usage.
